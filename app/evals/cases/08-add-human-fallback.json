{
  "id": "add-human-fallback",
  "version": "1.0.0",
  "goal": "Add a low-confidence human fallback with explicit side-effect review.",
  "app_mode": "advanced-chat",
  "fixture": {
    "snapshot_id": "chatflow-confidence-routing",
    "snapshot_version": "1.0.0",
    "allowed_resources": ["tool:human-queue"]
  },
  "allowed_capabilities": ["if-else", "tool", "skill:human-fallback"],
  "invariants": ["normal-answer-path-preserved", "external-test-requires-approval"],
  "required_changes": ["branch.low-confidence.added", "tool.human-queue.bound"],
  "forbidden_changes": ["draft-run.unapproved", "credential.read"],
  "expected_validation": "valid",
  "required_skill": "human-fallback",
  "side_effect_policy": {
    "allow_draft_run": false,
    "allowed_kinds": ["tool"],
    "max_draft_runs": 0
  },
  "expected_result": {
    "status": "completed",
    "reviewable": true,
    "final_valid": true,
    "changes": ["branch.low-confidence.added", "tool.human-queue.bound"],
    "unrelated_total": 10,
    "unrelated_preserved": 10,
    "unapproved_writes": 0,
    "incorrect_conflict_overwrites": 0,
    "trace": [
      {"type": "tool.completed", "message": "Loaded the human-fallback Skill without changing permissions."},
      {"type": "workspace.version.created", "message": "Added the low-confidence handoff branch."},
      {"type": "validation.passed", "message": "The handoff branch is structurally valid."},
      {"type": "review.ready", "message": "External Tool risk requires explicit approval."}
    ]
  }
}
