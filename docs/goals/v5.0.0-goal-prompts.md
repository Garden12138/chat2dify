# Chat2Dify v5.0.0 `/goal` Conversation Prompts

Paste one prompt into a new Codex task while on branch `v5.0.0`. The product
architecture is
[`docs/architecture/v5-ai-workflow-studio.md`](../architecture/v5-ai-workflow-studio.md)
and the execution ledger is [`docs/tasks.md`](../tasks.md).

## Phase 0 — Studio Home and product foundation

Status: `completed` on 2026-07-30. The prompt is retained as implementation
history; do not rerun it unless Phase 0 is explicitly reopened. Completion
evidence is in [`docs/tasks.md`](../tasks.md#6-phase-0--studio-shell-home-projects-identity-and-migration).

```text
/goal 实施 docs/tasks.md 的 v5 Phase 0：Studio Shell、Studio Home、Project、可信身份与迁移地基。

开始前按 AGENTS.md 顺序完整阅读 v5 产品架构、docs/tasks.md、v4 架构中需要复用的安全边界，以及本阶段触及的代码和测试；确认当前分支是 v5.0.0、工作区状态可控、v4 Release Gate 已完成。

只实施 P0-01 至 P0-08。目标不是只搭后端表，而是交付一个真实可用的产品切片：
- 默认关闭的 CHAT2DIFY_AI_STUDIO_V5_ENABLED 和 /api/v5/studio；
- 服务端可验证 Principal，浏览器传入的 user/role/project/app/environment 不得授权；
- Project/Membership 和个人项目迁移；
- Studio Shell 与 Home：最近应用、可恢复工作、搜索/筛选、真实空态/错误态、继续构建入口；
- SQLite 单用户 / PostgreSQL 团队持久化、增量迁移、Activity、Job/Outbox/Receipt 原语；
- 已填充 v4 SQLite 数据可安全迁移，v4 Deep Link 和 flag-off 回滚可用。

先把 Phase 0 标为 in_progress。以“登录 Dify → 打开 Studio Home → 找到已有应用/工作 → 打开 → 关闭 v5 回到 v4”为纵向验收路径。补齐身份伪造、Replay、跨项目、迁移、空态、错误态、权限态、可访问性和真实 Dify-host UI 测试。先运行 targeted tests，再运行完整支持套件和 git diff --check。只有产品路径和 P0 全部验收通过才标 completed，并记录命令、结果、视觉证据、决策与限制；否则保持 in_progress 或记录 blocked。不要开始 Phase 1，不要 commit、push、publish 或创建 PR。
```

## Phase 1 — Build Studio and candidate variants

```text
/goal 实施 docs/tasks.md 的 v5 Phase 1：Build Studio 与安全候选方案。

开始前按 AGENTS.md 完整阅读必读文件，确认 Phase 0 completed、当前分支为 v5.0.0，并核对 v4 Runtime、Workspace、Patch、ConfigPatch、Canvas Context、Validation、Review 和 Undo。

只实施 P1-01 至 P1-08：
- Composer、Goal Plan、Dify 选区、Node Inspector、Timeline、Business Preview、Validation、Risk、Technical Detail 的统一 Build Studio；
- “先解释”“给我两个方案”“基于两个方案再生成一个”的产品模式；
- 从同一 Base fork 2–3 个独立 Candidate Workspace；
- Business Behavior、Node/Edge、Model/Resource、Side Effect、Validation 的对比；
- Selected Node/Variable Flow/Resource Suggestion 等 Dify-native contextual commands；
- Chatbot、Completion、Agent 的 v5 创建路径；
- 为 v3 已支持节点补齐 v5 Typed Capability，加入受保护 node.remove，继续禁止 raw DSL/arbitrary JSON Patch；
- 不破坏无关布局的 Layout Preview。

主验收提示词：“为当前售后 Chatflow 提供两个低置信度兜底方案：人工接管和二次追问。”用户必须能在不打开 raw Plan/DSL 的情况下比较并选择一个有效方案，且 Dify 不发生写入。

先将 Phase 1 标为 in_progress；覆盖 Candidate 隔离/重建/合成、选区、安全上下文、配置型应用创建、节点负路径、删除前置条件、布局、取消、重启、Prompt Injection 和无写入。运行 targeted/full tests、固定可用性任务、真实 Dify-host Build Studio 和 git diff --check。全部通过后才标 completed。不要开始 Phase 2，不要 commit、push、publish 或创建 PR。
```

## Phase 2 — Blueprint Gallery

```text
/goal 实施 docs/tasks.md 的 v5 Phase 2：Blueprint Gallery 与引导式复用。

开始前按 AGENTS.md 完整阅读必读文件，确认 Phase 1 completed、当前分支为 v5.0.0，并核对 Capability Manifest、Patch Transaction、Policy、Candidate、Resource Catalog 和 Dify compatibility。

只实施 P2-01 至 P2-08：
- 以业务结果为中心的 Blueprint：预览图、兼容版本、Setup Schema、Capability/Resource、Cost/Risk、Validator、Scenario、Provenance、Version、Deprecation；
- Gallery 搜索/分类/兼容筛选/不可用原因；
- Model、Dataset、Tool、Trigger、Prompt、Variable 的 typed guided setup；
- Blueprint server-side 展开为一个正常 transactional Patch；
- 从选中节点保存 Private/Team Blueprint，必须定义 typed interface 并移除环境 ID/秘密；
- Blueprint Version/Upgrade Diff，绝不自动升级；
- 初始九个 Blueprint：Knowledge Retrieval、Human Fallback、JSON Extraction、Document Intake、Webhook Ingestion、Scheduled Report、Error/Retry、Model Routing、Support Classification；
- 真实 Gallery UI、空态、错误态、权限态、可访问性。

主验收场景：业务用户选择“knowledge retrieval with human fallback”，通过表单映射测试 Dataset 和 Review Channel，预览图与预期行为，然后应用成一个有效 Candidate；用户不编辑 JSON。

先将 Phase 2 标为 in_progress；覆盖恶意 Metadata、权限扩大、资源缺失、Secret Scan、版本升级、Patch Invariant、失败不移动 Head、无关图保持率和可用性。运行 targeted/full tests、真实 Workbench Gallery 和 git diff --check。全部通过后才标 completed。不要开始 Phase 3，不要 commit、push、publish 或创建 PR。
```

## Phase 3 — Scenario Lab and isolated Preview

```text
/goal 实施 docs/tasks.md 的 v5 Phase 3：Scenario Lab 与隔离候选 Preview。

开始前按 AGENTS.md 完整阅读必读文件，确认 Phase 2 completed、当前分支为 v5.0.0，并重点复核 v4 已确认的两个上游事实：Dify 1.14.2 忽略 Candidate Graph Draft Run 输入，且 Import 不支持可靠的上游 Idempotency。

只实施 P3-01 至 P3-08：
- Manual/Generated/Fixture/Approved Sanitized Run 来源的 Scenario/Dataset；
- Expected Output、Invariant、Rubric、Tags、Owner、Retention、Version；
- 在确定 Input Schema 后生成 Edge Cases，File 输入必须来自用户/批准 Fixture；
- 仅写专用非生产 Dify Target 的 Preview Environment；
- Production Credentials/Secret Mapping 在结构上不可解析；
- Intent/Receipt、Fixture Label/TTL、Idempotent Cleanup、Independent Absence Verification、Orphan Reaper、Ambiguous Reconciliation；
- 多 Candidate Scenario Run；
- Quality、Pass Rate、Regression、Latency、Model Usage、Cost Estimate、Side Effect 的 Side-by-side Comparison；
- 与 Candidate/Mapping/Suite/Policy/Expiry 精确绑定的 Baseline 和 Regression Gate。

主验收场景：对 Phase 1 的两个兜底 Candidate 运行售后回归集，比较质量、时延、模型用量、人工升级次数和成本估算，保存选中基线，并独立确认 Preview 临时 App 已删除。

先将 Phase 3 标为 in_progress；不要通过临时写生产 Draft 或伪造 Candidate Run 通过验收。覆盖 Dataset Prompt Injection、Secret Scan、File Boundary、Restricted Mapping、Side-effect Approval、Budget、Timeout、Cancel、Import Ambiguity、No Blind Retry、Cleanup、Metric Determinism、Stale Evidence、Accessibility。运行 targeted/full tests、真实隔离 Preview 验收和 git diff --check。全部通过并完成清理后才标 completed。不要开始 Phase 4，不要 commit、push、publish 或创建 PR。
```

## Phase 4 — Review and Release Center

```text
/goal 实施 docs/tasks.md 的 v5 Phase 4：协作 Review 与 Release Center。

开始前按 AGENTS.md 完整阅读必读文件，确认 Phase 3 completed、当前分支为 v5.0.0，并核对 Approval/Commit/Publish/Hash、Scenario Evidence、Project Role、External Receipt 和 Dify compatibility。

只实施 P4-01 至 P4-09：
- Author/Reviewer、Assignment、Comments、Request Changes、Approve/Reject/Supersede/Expiry；
- 可选 Author/Approver Separation；
- 从批准 Candidate 生成 canonical、immutable、secrets-free Workflow Artifact；
- Logical App 与可选 Dev/Staging/Production Environment；
- Model/Dataset/Tool/Trigger/Credential Availability 的 opaque Mapping；
- Deployed Base + Target Drift + Proposed Artifact + Scenario Evidence + Risk + Release Note 的 Release Preview；
- Apply Draft：精确绑定 Artifact/Environment/Mapping/Policy/Target Hash，复用安全 Commit；
- Explicit Publish：与 Apply 分离的独立高风险用户动作，Builder/MCP/Repair 不得自主 Publish；
- Release History、Receipt、Dify Hash、Rollback Proposal；
- 可选 deterministic secrets-free Git serialization，Pull 内容重新走 Change Request。

主验收场景：Reviewer 请求一次修改并批准修正后的已测试 Candidate；Releaser Apply 到 Staging；验证后 Publish 需要第二个显式动作；Rollback 是新的 Review/Release 并保留外部 Drift。

先将 Phase 4 标为 in_progress；覆盖跨项目、Self Approval、Stale Decision、Secret Scan、Canonical Bytes、Mapping Mismatch、Drift、Duplicate/Ambiguous Outcome、Apply/Publish Separation、Rollback、Git Conflict、Accessibility 和真实 Dify-host Release Center。运行 targeted/full tests、真实 Dify/Git 验收和 git diff --check。全部通过后才标 completed。不要开始 Phase 5，不要自行 commit、push、merge、publish 或创建 PR；这里的 Publish 只指产品功能的受控测试实现，不授权你发布当前仓库或镜像。
```

## Phase 5 — Run Center and repair proposals

```text
/goal 实施 docs/tasks.md 的 v5 Phase 5：Run Center、Repair Proposal 与安全自动化。

开始前按 AGENTS.md 完整阅读必读文件，确认 Phase 4 completed、当前分支为 v5.0.0，并核对 Execution Observation、Artifact/Release Receipt、Scenario、Change Request、Worker/Outbox 和所有授权边界。

只实施 P5-01 至 P5-08：
- Dify Execution 与 Project/Logical App/Environment/Artifact/Release 的关联；
- Status、Failed Node、Stable Error、Latency、Model Usage/Cost、Sanitized Shape；
- Run Center：Success/Error Trend、Release Overlay、Regression、Error Cluster、Slow/Costly Path；
- Incident Detail：Execution Evidence、Release Diff、Scenario Coverage、Known Error、Affected Node；
- “创建修复方案”：以脱敏证据预填 Change Request，仍需 Build → Scenario → Review → Apply → Publish；
- Alert Adapter 与 Scheduled Scenario Regression；
- Lease/Outbox Worker、Heartbeat、Cancellation、Bounded Retry、Dead-letter/Reconciliation、Receipt；
- OAuth/Scoped Token MCP/API：只允许 Search/Inspect/Create Change/Typed Propose/Scenario Run-Read/Review Read/Release Preview；
- 禁止 MCP Approval/Apply/Publish/Credential Plaintext/Raw DSL/Arbitrary Patch。

主验收场景：Run Center 把生产 Variable Reference Failure 关联到已发布 Artifact，解释受影响路径并创建 Repair Proposal；修复通过 Scenario 和正常 Review/Release；Scoped MCP 只能查看结果，不能批准、Apply、Publish 或读取秘密。

先将 Phase 5 标为 in_progress；覆盖 Correlation、Redaction、Clustering、Repair Linkage、Alert Dedup、Worker Loss、Lease Expiry、Outbox Recovery、MCP Scope Escalation、Token Rotation/Revocation、Cross-project Search、Rate Limit、Accessibility。运行 targeted/full tests、多 Worker PostgreSQL、真实 MCP Client、真实 Incident-to-Repair 用户路径和 git diff --check。全部通过后才标 completed。不要开始 Release Gate，不要 commit、push、publish 或创建 PR。
```

## v5.0.0 Release Gate

```text
/goal 执行 docs/tasks.md 的 v5.0.0 Release Gate。

开始前按 AGENTS.md 完整阅读必读文件，确认 Phase 0 至 Phase 5 全部 completed、当前分支为 v5.0.0、工作区范围清晰。

Release Gate 只允许发布核验、阻断缺陷修复、迁移/回滚、安全/性能/兼容/真实环境验收、文档与版本一致性修复。不得新增产品范围。

逐项完成 docs/tasks.md Section 12：
- Signed-in Dify Studio Home → Build Studio；
- Workflow/Chatflow/Chatbot/Completion/Agent 新建和修改；
- Candidate Comparison；
- Blueprint Discover/Setup/Apply/Upgrade；
- Scenario Authoring/Preview/Comparison/Baseline/Regression/Cleanup；
- Review/Request Changes/Approval/Apply/Explicit Publish/Rollback；
- Run Incident → Repair → Scenario → Review → Release；
- v5 flag-off → v4；
- 固定 Usability、Accessibility、Responsive；
- Project Isolation、Approval/Publish 权限、Secret Scan、Conflict、Preview Mapping；
- Candidate Validity、Unrelated Preservation、Blueprint Success、Scenario Success、Incident Repair；
- populated v4 SQLite migration、PostgreSQL、Outbox/Lease/Recovery；
- full v3/v4/v5 tests；
- real Dify/DSL、Preview、Git、MCP、Dify-hosted Studio；
- rollout/rollback、git diff --check 和最终 Diff 审查。

任何必须项失败就保持 in_progress 或标 blocked 并记录证据，不得降低门槛。全部通过才标 completed，并记录命令、结果、视觉/可用性、环境、版本、清理、限制与回滚证据。不要自行 commit、push、merge、发布仓库/镜像或创建 PR。
```
